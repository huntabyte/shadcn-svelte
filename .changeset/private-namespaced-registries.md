---
"shadcn-svelte": minor
---

Add support for namespaced registries in `components.json`. Install items with `add @namespace/name`, including private registries with environment-based headers and query parameters. Resolve dependencies across registries using each namespace's credentials.
