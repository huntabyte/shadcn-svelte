# shadcn-svelte docs

## Run locally

From the repository root:

```bash
pnpm install
pnpm -F docs dev:svelte
```

Open <http://127.0.0.1:5173/docs/components/attachment> and replace `attachment` with `bubble`, `marker`, `message`, `message-scroller`, or `typeset` to inspect the local component previews.
